import React from 'react';

import Layout from '../layout';
import Projects from '../work-projects';

const Home = () => (
    <Layout>
        <Projects />
        
    </Layout>
)

export default Home;
